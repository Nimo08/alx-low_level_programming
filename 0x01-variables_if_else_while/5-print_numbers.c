#include <stdio.h>

/**
 * main - Entry point
 * Description: prints base 10 numbers from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
