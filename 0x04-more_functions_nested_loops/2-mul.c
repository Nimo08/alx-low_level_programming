#include "main.h"
/**
 * mul - multiplies two integers
 * @a: int to be multiplied
 * @b: int to be multiplied
 * Return: 0
 */
int mul(int a, int b)
{
	int prod = 0;

	prod = (a * b);
	return (prod);
}

